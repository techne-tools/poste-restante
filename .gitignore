# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.tsbuildinfo
*.local

# Environment / secrets
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Impeccable tooling — reinstall per worktree via: npx impeccable install
.agents/skills/
.codex/
.opencode/

# Design & process docs
# Design truth (PRODUCT, DESIGN, CONTRACT, .impeccable) is COMMITTED — the pipeline
# requires it for worktree agents (FLEET 2026-08-10). Process docs (PIPELINE, TASK,
# RECON, DEVISING, PLAN-*) stay local-only — they carry personal details and machine names.
DEVISING.md
PIPELINE.md
PLAN-*.md
BRIEF-*.md
RECON.md
TASK.md
REVIEW-*.md

# agent-orchestrator station output captures
.ao-capture/

# launch listing drafts
itch-listing-draft.md
producthunt-launch-draft.md
