# Poste Restante reference client — public env (NO SECRETS).
# The client carries no credentials: auth lives in the browser and travels
# to the house through the nginx proxy.

# Host port for the composed client (probed free 2026-09-07; 21018 is immich,
# 21019 vaultwarden on horza).
CLIENT_PORT=21034
