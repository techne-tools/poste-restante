# MinIO public defaults (committed). Non-secrets only.
# Secrets (MINIO_ROOT_PASSWORD) live in .env.enc (sops+age).

MINIO_ROOT_USER=poste_minio
MINIO_BROWSER=on
MINIO_PORT_API=21018
MINIO_PORT_CONSOLE=21019
MINIO_BUCKET=letters
